class Student {
    constructor(id, firstName, lastName, classEnrolled, age, year){
        this.id = id, 
        this.firstName = firstName,
        this.lastName = lastName
        this.classEnrolled = classEnrolled,
        this.year = year, 
        this.age = age
    }
}